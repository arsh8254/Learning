import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <div>
    {/* multiple elements must be stored in a single <div> element */}
    <h1>Hello World!</h1>
    <p>This is a paragraph.</p>
  </div>,
  document.getElementById("root")
);
